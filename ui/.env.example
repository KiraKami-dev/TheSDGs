VITE_API_BASE=http://localhost:8020/api

# URL where the Streamlit dashboard (OVERVIEW/app.py) is served.
# The "Overview dashboard" button in the UI opens this. Change only if you
# run Streamlit on a different host/port than the default below.
VITE_DASHBOARD_URL=http://localhost:8501
